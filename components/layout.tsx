"use client";
import Link from "next/link";
import { Heart, House, ListMusic, Search, ListPlus } from "lucide-react";
import { MusicPlayer } from "@/components/music-player";
const links = [{ href: "/", label: "Home", icon: House }, { href: "/search", label: "Search", icon: Search }, { href: "/favorites", label: "Your Library", icon: Heart }, { href: "/queue", label: "Queue", icon: ListPlus }];
export function Layout({ children }: { children: React.ReactNode }) { return <div className="min-h-screen bg-background pb-32 md:pl-64"><aside className="fixed inset-y-0 left-0 z-30 hidden w-64 border-r border-white/5 bg-black p-6 md:block"><Link href="/" className="mb-10 flex items-center gap-2 text-2xl font-black tracking-tight"><span className="grid size-9 place-items-center rounded-full bg-brand text-black"><ListMusic /></span> sonora</Link><nav className="space-y-2">{links.map(({ href, label, icon: Icon }) => <Link key={href} href={href} className="flex items-center gap-4 rounded-lg px-3 py-3 text-sm font-bold text-zinc-400 transition hover:bg-white/10 hover:text-white"><Icon size={21}/>{label}</Link>)}</nav></aside><main className="mx-auto max-w-7xl px-5 py-6 md:px-10 md:py-10">{children}</main><nav className="fixed inset-x-0 bottom-20 z-20 flex justify-around border-t border-white/10 bg-zinc-950/95 py-2 backdrop-blur md:hidden">{links.map(({ href, label, icon: Icon }) => <Link key={href} href={href} className="flex flex-col items-center gap-1 text-[10px] text-zinc-400"><Icon size={20}/>{label}</Link>)}</nav><MusicPlayer /></div> }
